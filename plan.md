# 和风优先的全局双源融合优化执行清单

更新日期：2026-07-25

目标：所有天气入口保持全局融合；和风为主源，彩云只补充 API 原生缺失字段。彩云综合响应按坐标缓存 1 小时，并在当前单容器内进行并发去重。

## 执行任务

- ✅ 建立实施清单并确认基线：现有 `unittest` 10 项全部通过。
- ✅ P0：为共享缓存增加异步 single-flight、取消隔离和成功结果缓存。
- ✅ P0：彩云综合接口改为 `/weather`，请求 `72h/15d/alert/metric:v2`，缓存原始响应 3600 秒并增加失败冷却。
- ✅ P1：移除和风 10 分钟组合缓存，按 endpoint TTL 缓存并在 Geo 后并行获取所需数据。
- ✅ P1：增加 `full/hourly/daily/rain/indices` profile，以及只刷新和风的 `refresh_qweather`。
- ✅ P1：修正融合优先级、分钟降水来源、缺失值、降水单位、风速、气压、预警和字段来源。
- ✅ P1：接入彩云实况/小时/逐日的套餐内补充字段和日级 4 项生活指数。
- ✅ P2：Bot handler 与 scheduler 共用天气/LLM 服务，并在 shutdown 关闭 HTTP/Redis 客户端。
- ✅ P2：后台任务按唯一地点复用天气和日报结果。
- ✅ P2：图表缓存加入数据指纹，修复 `/chart`、`/report` 多词地点。
- ✅ 更新 README、`.env.example`、命令/API 映射与双源策略文档。
- ✅ 增加缓存、并发、失败降级、刷新、字段语义、生命周期和命令测试。
- ✅ 使用 `uv run python -m unittest discover -s tests -v` 完成最终验证：28 项测试通过，并完成 compileall、Ruff F/E9、`git diff --check` 与应用工厂烟测。

## 固定决策

- 彩云成功缓存 TTL：3600 秒，固定 TTL，不滑动续期。
- 彩云临时失败冷却：60 秒；鉴权、额度类失败冷却：3600 秒。
- 彩云缓存 key：规范化经纬度 + API/单位/步长/预警版本。
- 刷新按钮绕过和风天气缓存，但不绕过和风 Geo 和彩云缓存。
- single-flight 仅保证当前 Python 进程；Redis 分布式锁和历史天气不在本轮范围。
- 自动测试不得调用真实彩云 API。

## 图表视觉升级（2026-07-25）

- ✅ 渲染并检查现有温度图、降水图的 24 小时视觉基线。
- ✅ 建立两张图共用的深色天气卡片主题、时间轴与信息层级。
- ✅ 升级逐小时温度图：关键值标注、体感缺失断点、日期分隔和可读温标。
- ✅ 升级逐小时降水图：概率/降水量双尺度、正确单位、缺失值与无雨状态。
- ✅ 将图表缓存版本升级并补充视觉语义测试。
- ✅ 生成新版预览并完成 375px 手机宽度视觉 QA、单元测试和静态检查。

## Telegram Bot API 跟进（2026-07-25）

- ✅ 核对官方版本：Telegram Bot API 当前为 10.2；PTB 22.8 稳定版及开发分支仅完整支持到 10.0。
- ✅ 将 `python-telegram-bot` 从 22.5 升级到 22.8，并记录客户端/协议兼容版本。
- ✅ 审计 22.6–22.8 的破坏性变更及本项目调用点。
- ✅ 补充本地兼容说明，明确 10.1 Rich Messages 与 10.2 Ephemeral Messages 的接入边界。
- ✅ 在 PTB 22.8 环境完成 33 项全量测试、应用工厂烟测和静态检查。

## OpenAI SDK 与 GPT-5.6 升级（2026-07-25）

- ✅ 使用官方最新版解析器确认目标为 `gpt-5.6-sol`，并读取迁移与提示词指南。
- ✅ 清点当前用法：单一天气日报路由、Responses API、`medium` 推理，无工具/状态/多模态/显式缓存。
- ✅ 将 OpenAI Python SDK 精确锁定到当前最新版 2.48.0。
- ✅ 将活动默认模型从 `gpt-5.5` 迁移到 `gpt-5.6-sol`，保留 Responses API、`medium` 与现有提示词。
- ✅ 补充 GPT-5.6 推理参数校验、配置示例、README 和本地迁移文档。
- ✅ 使用模拟 SDK 响应验证请求结构，并完成 37 项全量测试、烟测和静态检查。

## 跨日详情与预警展示修复（2026-07-26）

- ✅ 以天气数据的本地 `update_time` 为基准，统一选择当天或首个未来逐日预报。
- ✅ 让默认天气、逐日查询、逐日图表和 AI 日报过滤过期日级数据，禁止把昨日预报标成“今日”。
- ✅ 将和风预警颜色/严重度规范化为中文，未分级值不展示，标题已有等级时不重复。
- ✅ 增加跨午夜、过期预报、预警等级和 LLM 一致性测试；44 项全量测试、compileall、Ruff F/E9、`git diff --check` 和应用工厂烟测全部通过。
